# تعليمات إعداد مفتاح Gemini API

## الخطوات المطلوبة:

### 1. الحصول على مفتاح API من Google AI Studio

1. قم بزيارة [Google AI Studio](https://makersuite.google.com/app/apikey)
2. قم بتسجيل الدخول بحساب Google الخاص بك
3. انقر على "Create API Key" أو "Get API Key"
4. اختر مشروع Google Cloud أو أنشئ مشروعًا جديدًا
5. انسخ المفتاح الذي تم إنشاؤه

### 2. إضافة المفتاح إلى التطبيق

افتح الملف التالي:
```
lib/core/services/ai_service.dart
```

ابحث عن السطر:
```dart
static const String _apiKey = 'YOUR_GEMINI_API_KEY_HERE';
```

استبدل `YOUR_GEMINI_API_KEY_HERE` بالمفتاح الذي حصلت عليه:
```dart
static const String _apiKey = 'AIzaSy...'; // المفتاح الخاص بك
```

### 3. تشغيل التطبيق

بعد إضافة المفتاح، قم بتشغيل:
```bash
flutter pub get
flutter run
```

## ملاحظات مهمة:

- ⚠️ **لا تشارك مفتاح API الخاص بك علنًا**
- 💡 للإنتاج، استخدم متغيرات البيئة بدلاً من تضمين المفتاح مباشرة في الكود
- 📊 تحقق من حدود الاستخدام المجاني في [Google AI Studio](https://ai.google.dev/pricing)

## الميزات المتاحة:

1. **توليد أفكار للدروس**: احصل على أفكار إبداعية وأنشطة تفاعلية
2. **كتابة اختبارات سريعة**: توليد أسئلة متنوعة (اختيار من متعدد، صح وخطأ)
3. **تلخيص ملاحظات الفصل**: تحويل الملاحظات المبعثرة إلى ملخص منظم

## استكشاف الأخطاء:

### خطأ: "API key not valid"
- تأكد من نسخ المفتاح بشكل صحيح
- تحقق من تفعيل Gemini API في مشروع Google Cloud

### خطأ: "Quota exceeded"
- تحقق من حدود الاستخدام في حسابك
- انتظر حتى يتم تجديد الحصة (عادة يوميًا)

### خطأ في الاتصال
- تأكد من اتصال الإنترنت
- تحقق من إعدادات الجدار الناري
